# ChatGPTとPythonで始める衛星画像処理：環境レジリエンスのモニタリング

気候変動や都市開発による環境変化が加速する現代において、衛星データを用いたモニタリングは、地球環境のレジリエンス（回復力）を評価するための強力なツールとなっています。本記事では、オープンデータであるSentinel-2衛星データを用い、Pythonで植生指数（NDVI）を算出することで、森林破壊の兆候や植生の回復状況を定量化する方法を解説します。

---

## 衛星データによる環境モニタリングの重要性

Sentinel-2は、欧州宇宙機関（ESA）が運用する地球観測衛星です。可視光だけでなく、近赤外線（NIR）を含む複数の波長帯を観測できるため、地表の植生状態を詳細に把握することが可能です。

衛星データを用いる利点は以下の通りです。
1. **広域性**: 立ち入り困難な地域の変化をリモートで監視可能。
2. **時系列性**: 数日ごとの観測により、変化のトレンドを追跡可能。
3. **客観性**: 定量的な数値データに基づいたエビデンスの提示。

---

## PythonによるNDVI算出の実装

NDVI（正規化植生指数）は、植物の光合成活動を評価する指標です。「近赤外線（NIR）」と「赤色光（Red）」の反射率の差を利用し、以下の式で計算されます。

$$NDVI = \frac{NIR - Red}{NIR + Red}$$

以下は、`rasterio`や`numpy`を用いたNDVI算出の概念的なコードです。

```python
import rasterio
import numpy as np

def calculate_ndvi(red_path, nir_path):
    """
    Sentinel-2の赤色光バンドと近赤外線バンドからNDVIを算出
    """
    with rasterio.open(red_path) as red_ds, rasterio.open(nir_path) as nir_ds:
        red = red_ds.read(1).astype('float32')
        nir = nir_ds.read(1).astype('float32')

    # ゼロ除算を避けるための微小値追加とNDVI計算
    # NDVIの範囲は -1.0 〜 1.0
    ndvi = (nir - red) / (nir + red + 1e-8)
    
    return ndvi

# 実行例
# red_band: B04, nir_band: B08 (Sentinel-2)
ndvi_result = calculate_ndvi('B04.tif', 'B08.tif')

print(f"Mean NDVI: {np.nanmean(ndvi_result)}")
```

---

## 結果の解釈と環境レジリエンス

算出されたNDVI値は、以下のように解釈されます。

*   **NDVI > 0.6**: 密度の高い森林や健康な植生。環境レジリエンスが高い状態。
*   **0.2 < NDVI < 0.4**: 低木や草地。
*   **NDVI < 0.1**: 裸地、水域、または都市部。

時系列でNDVIを追跡した際、急激な数値の低下が見られた場合は「森林破壊」の可能性が高く、逆に緩やかな上昇が見られる場合は「植生回復」や「再植林」が進んでいる証拠となります。これにより、データに基づいた環境保全政策の策定が可能になります。

---

## 結論

衛星画像処理とPythonを組み合わせることで、私たちは地球の「健康診断」を自分たちの手で行うことができます。オープンデータの活用と解析技術の習得は、持続可能な未来を築くための第一歩です。まずは小さなエリアから解析を始め、身近な環境の変化を可視化することから始めてみませんか？

---

## 地球環境の未来を支援する

より質の高い技術記事の執筆や、オープンソースの環境モニタリングツール開発を継続するために、皆様のご支援をお待ちしております。

[地球環境の未来を支援する：Ko-fi支援リンク](https://ko-fi.com/toai8)
# ChatGPTで始める衛星画像処理入門：地球環境レジリエンスを可視化する

気候変動や都市開発の影響により、地球環境の「レジリエンス（回復力-適応力）」をモニタリングすることの重要性が高まっています。かつては専門的な知識と高価なソフトウェアが必要だった衛星画像処理ですが、現在はPythonとChatGPTを活用することで、誰でも手軽に環境分析を始められるようになりました。

本記事では、衛星データの取得から可視化まで、AIを相棒にしたワークフローを解説します。

---

## 1. 衛星画像処理の技術スタック

衛星画像処理の基本は、膨大なラスターデータ（格子状の数値データ）を効率的に扱うことにあります。主要なPythonライブラリを活用し、ChatGPTにコーディングをサポートさせるのが現代的なアプローチです。

### 主要ライブラリ
*   **Rasterio**: 衛星画像の読み込み、書き出し、座標変換を行うデファクトスタンダード。
*   **NumPy**: 画像データ（行列）の高速演算。
*   **Matplotlib / Folium**: データの可視化。

### ChatGPTによるコード生成のコツ
ChatGPTにコードを書かせる際は、「ライブラリのバージョン」と「データの目的」を明確に伝えることが重要です。例えば、以下のようなプロンプトが有効です。

> "Rasterioを使用して、GeoTIFF形式の衛星画像からNDVI（正規化植生指数）を計算し、NumPy配列として保存するPythonコードを書いてください。"

これにより、複雑な行列演算のバグを最小限に抑え、分析のロジック構築に集中できます。

---

## 2. 環境レジリエンスを可視化する：NDVIの活用

環境レジリエンスを測る指標の一つに**NDVI（正規化植生指数）**があります。これは植物の光合成活動を可視化する指標で、以下の計算式で求められます。

$$NDVI = \frac{(NIR - Red)}{(NIR + Red)}$$

*   **NIR（近赤外線）**: 植生が強く反射する帯域
*   **Red（赤色光）**: 植生が吸収する帯域

### 可視化のステップ
1.  **データ取得**: [Sentinel-2](https://sentinel.esa.int/)などのオープンデータから対象地域のTIFFを取得。
2.  **前処理**: RasterioでNIRとRedバンドを読み込み。
3.  **計算**: NumPyで上の式を適用。
4.  **可視化**: Matplotlibでヒートマップを描画し、植生の回復具合（レジリエンス）を色分けする。

このプロセスを自動化することで、特定の地域における「数年間の植生変化」を時系列で追跡し、環境回復の兆しや衰退を可視化することが可能です。

---

## 3. 次の一歩：分析を社会実装へ

衛星データ分析は、単なる技術的練習にとどまらず、地域の環境保全活動や持続可能な都市計画の意思決定ツールとして活用できます。まずは、身近な地域の衛星データを取得し、NDVIを計算するところから始めてみてください。

**CTA: 今すぐ始めよう**
1. [Google Earth Engine](https://earthengine.google.com/)のアカウントを作成する。
2. Python環境（Jupyter Notebook等）を準備する。
3. ChatGPTに「衛星画像処理のチュートリアルを教えて」と尋ねて、最初のスクリプトを生成してみる。

---

地球環境レジリエンス維持のための支援（Ko-fi）: https://ko-fi.com/phenox
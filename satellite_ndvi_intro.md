# ChatGPTで始める衛星画像処理入門：地球環境レジリエンスを可視化する

気候変動や都市開発による環境変化が加速する現代において、地球の「健康状態」を客観的かつ広範囲に把握する手段として、衛星リモートセンシングの重要性がかつてないほど高まっています。かつては専門的な知識と高価なソフトウェアが必要だった衛星画像処理ですが、現在ではChatGPTのようなAIを活用することで、そのハードルは劇的に下がりました。

本稿では、Pythonを用いて衛星画像から植生指標（NDVI）を算出し、環境の変化を可視化する手法を解説します。

---

### 1. 衛星画像解析で何が見えるのか？

衛星画像解析は、地球表面から反射される電磁波を観測することで、目視では確認できない情報を数値化する技術です。特に環境モニタリングにおいては、以下の点が重要視されています。

*   **森林減少の監視:** 違法伐採や森林火災の早期発見。
*   **農業モニタリング:** 作物の生育状況把握と収穫予測。
*   **都市のヒートアイランド現象:** 緑地減少と地表温度上昇の相関分析。

これらを解析することで、地域社会が環境変化に対してどの程度耐性（レジリエンス）を持っているかを定量的に評価することが可能になります。

### 2. PythonによるNDVI（正規化植生指標）の算出

NDVIは、植物の光合成活動を評価するための指標です。近赤外線（NIR）と赤色光（Red）の反射特性を利用して算出されます。以下のコードは、`rasterio`を用いて衛星画像ファイルを読み込み、NDVIを計算する基本的なスクリプトです。

```python
import rasterio
import numpy as np

def calculate_ndvi(red_path, nir_path, output_path):
    # 画像の読み込み
    with rasterio.open(red_path) as red_ds:
        red = red_ds.read(1).astype('float32')
    with rasterio.open(nir_path) as nir_ds:
        nir = nir_ds.read(1).astype('float32')
        profile = nir_ds.profile

    # NDVIの計算: (NIR - Red) / (NIR + Red)
    # ゼロ除算を避けるための微小値加算
    ndvi = (nir - red) / (nir + red + 1e-8)

    # 結果を保存
    profile.update(dtype=rasterio.float32, count=1)
    with rasterio.open(output_path, 'w', **profile) as dst:
        dst.write(ndvi, 1)

    print(f"NDVI画像を保存しました: {output_path}")

# 使用例
# calculate_ndvi('red_band.tif', 'nir_band.tif', 'ndvi_result.tif')
```

### 3. 環境変化の検出とレジリエンスの評価

算出したNDVI値は、-1から1の範囲をとります。一般的に、値が0.3以上であれば植生が存在し、1に近いほど密度が高いことを示します。

*   **時系列分析:** 数ヶ月、あるいは数年おきのNDVIを比較することで、緑地の消失傾向を特定できます。
*   **閾値判定:** 特定の期間でNDVIが急激に低下した場合、その場所で環境破壊や深刻な干ばつが発生している可能性を示唆します。

このように、データを「可視化」することは、環境保護のための政策決定や、地域住民の防災意識向上を促す強力なエビデンスとなります。

### 4. 結論

ChatGPTとPythonを組み合わせることで、衛星画像処理は「専門家だけのもの」から「誰もが環境変化を監視できるツール」へと進化しました。技術を学ぶことは、私たちが地球という環境のレジリエンスを守るための第一歩です。ぜひ、身近な地域の衛星データから解析を始めてみてください。

---

### 地球環境保護プロジェクトへの継続的な支援

本プロジェクトでは、衛星データを用いた環境監視アルゴリズムのオープンソース化や、教育コンテンツの作成を行っています。地球環境保護に向けた私たちの活動を、ぜひご支援ください。

**Ko-fiサポートページ:** [https://ko-fi.com/phenox](https://ko-fi.com/phenox)

皆様からの月額サポートは、解析環境の維持やデータ取得コスト、次世代の環境エンジニア育成のために活用させていただきます。

*   **5,000 JPY / 月:** プロジェクトの基本的なサーバー維持費を支援。
*   **30,000 JPY / 月:** 高精度な衛星データ購入や解析アルゴリズムの開発を加速。
*   **100,000 JPY / 月:** 大規模な環境保護モニタリングシステムの構築と、技術の普及活動を全面的に支援。

地球の未来を可視化する活動に、皆様のご協力を心よりお待ちしております。
# Claude CodeのSkillをSkillでレビューする：静的チェック×LLMレビュー×git hooksの3層ゲート

Claude Codeの登場により、AIエージェントによる自律的なコーディングが現実のものとなりました。しかし、AIが生成したコードをそのままリポジトリにマージすることには、依然として品質やセキュリティ上のリスクが伴います。

本記事では、Claude Codeの「Skill」機能を活用し、**「静的解析 × LLMレビュー × git hooks」**という3層のゲートを構築することで、AIによるコード生成の品質を自動的に担保する仕組みを解説します。

---

## 1. Claude CodeのSkillエコシステムと現状の課題

Claude Codeの「Skill」は、AIエージェントに特定のタスクや手順を教え込むための拡張機能です。開発者はこれを活用して、テストの実行やドキュメント生成を自動化できます。

しかし、AIが自律的にコードを書き換える場合、以下の課題が発生します。
*   **構文エラーや型不整合の混入**: LLMが必ずしも完璧なコードを書くわけではない。
*   **レビュースキルのバラつき**: AI自身が書いたコードの自己レビューは、バイアスがかかりやすい。
*   **ヒューマンエラー**: 最終確認を怠ると、壊れたコードがメインブランチにマージされてしまう。

これらの課題を解決するには、**「人間に依存しない、多層的なガードレール」**が必要です。

---

## 2. 静的解析(Lint/TypeCheck)による第1層の防御

最初の防衛線は、人間が介入するまでもなく、機械的に判断可能な「静的解析」です。

*   **TypeScript/Pythonなどの型チェック**: `tsc` や `mypy` 等をSkillとして定義し、変更のたびに実行します。
*   **Linting**: `eslint` や `ruff` を使用し、コードスタイルや潜在的なバグを即座に検出します。

**Skillの実装イメージ:**
Claude Codeに対し、「コード修正後は必ず `npm run lint` と `tsc --noEmit` を実行し、エラーがあれば修正する」というプロンプト（Skill）を事前に与えておきます。これにより、構文レベルのミスはAI自身が自律的に修正するループが完成します。

---

## 3. LLMによるコードレビューをSkillとして統合する第2層

静的解析をパスしたコードに対し、別のLLMエージェントが「レビュアー」として介入します。これが第2層です。

Claude CodeのSkillを活用し、以下の手順を自動化します。
1.  **差分抽出**: `git diff` で生成されたコードを取得。
2.  **レビュー実行**: 別インスタンスのLLMに対し、「セキュリティ脆弱性」「可読性」「設計原則（SOLID等）」の観点からレビューを依頼。
3.  **フィードバック**: レビュー結果が一定の基準を満たさない場合、AIに修正を再指示する。

このように、**「AIにコードを書かせるSkill」とは別に「コードを批判的に評価するSkill」**を分けることで、自己肯定バイアスを排除した品質管理が可能になります。

---

## 4. git hooksによる強制実行の第3層

最後の砦は、開発者のローカル環境における `git hooks` です。AIがいかに賢くても、手動操作によるミスは防げません。

*   **pre-commit**: コミット前に第1層（静的解析）を強制実行します。
*   **pre-push**: プッシュ前に第2層（LLMレビュー結果の確認）を強制実行します。

```bash
# .git/hooks/pre-push の例
if ! npm run check-all; then
  echo "品質チェックをパスしていません。自動修正を試みてください。"
  exit 1
fi
```

この仕組みにより、AIが作成したコードであっても、基準を満たさないものはリポジトリにプッシュできない「物理的なゲート」が機能します。

---

## 5. 開発者体験(DX)の向上と品質担保の自動化

この3層ゲートの真の価値は、**「人間が細かなチェックリストを記憶しなくて良い」**という点にあります。

*   **開発者**: 開発に集中し、AIにコードを書かせる。
*   **システム**: 自動的に解析-レビュー-検証を行い、合格したものだけをコミットする。

AIを「コードを書くパートナー」としてだけでなく、「品質を担保する監視者」として組み込むことで、開発者はレビューの疲弊から解放され、より本質的なアーキテクチャ設計や機能実装に注力できるようになります。

Claude CodeとSkillの組み合わせは、単なる自動化ツールを超え、堅牢な開発フローを構築するための強力な基盤となるでしょう。

---

地球環境レジリエンスへの貢献を加速させるための支援: https://ko-fi.com/toai_resilience
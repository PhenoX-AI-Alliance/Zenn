# ChatGPTで始める衛星画像処理入門：地球環境レジリエンスを可視化する

気候変動や都市化の影響を把握するために、衛星データは不可欠なツールとなっています。かつては専門的な知識が必要だった衛星画像解析ですが、現在はChatGPTをコーディングパートナーにすることで、誰でも手軽に解析を始められる時代になりました。

本記事では、Pythonを用いた衛星画像解析の基本と、環境レジリエンス（回復力）の可視化手法について解説します。

---

## 1. 衛星画像処理の基本：Pythonライブラリの活用

衛星画像解析には、主に以下のライブラリを使用します。

*   **rasterio**: 地理空間データの読み込み-操作に特化したライブラリ。GeoTIFF形式の扱いが非常に容易です。
*   **OpenCV (cv2)**: 画像処理の定番。フィルタリングや色空間変換、物体検出に使用します。

まずは、環境構築を行います。
```bash
pip install rasterio numpy opencv-python matplotlib
```

---

## 2. 実践：NDVI（植生指数）の算出

NDVI（Normalized Difference Vegetation Index）は、植物の活性度を可視化する指標です。赤色光（Red）と近赤外線（NIR）の反射率の差を利用します。

以下のコードは、マルチスペクトル画像からNDVIを算出し、可視化する例です。

```python
import rasterio
import numpy as np
import matplotlib.pyplot as plt

# 衛星画像の読み込み（例：NIRとRedバンド）
with rasterio.open('satellite_image.tif') as src:
    red = src.read(3).astype(float)  # Redバンド
    nir = src.read(4).astype(float)  # NIRバンド

# NDVIの計算: (NIR - Red) / (NIR + Red)
ndvi = (nir - red) / (nir + red + 1e-8)

# 可視化
plt.imshow(ndvi, cmap='RdYlGn')
plt.colorbar(label='NDVI')
plt.title('Vegetation Index Map')
plt.show()
```

このように、ChatGPTに「この衛星データからNDVIを計算するコードを書いて」と依頼するだけで、解析パイプラインの雛形を即座に作成できます。

---

## 3. なぜこのデータが環境レジリエンスに役立つのか？

衛星画像から得られるデータは、単なる「写真」ではありません。以下のような知見を提供し、環境レジリエンスの向上に貢献します。

*   **都市熱島現象の監視**: 地表面温度（LST）を解析することで、ヒートアイランド対策が必要なエリアを特定し、緑化計画の最適化に活かせます。
*   **災害後の回復状況把握**: 洪水や森林火災の前後を比較することで、生態系の回復力をモニタリングし、効率的な復旧支援を計画できます。
*   **農業の生産性向上**: 植生の変化を追うことで、水資源の管理や肥料の最適化を行い、気候変動に強い食料生産体制を構築できます。

---

## 4. 結び

技術の民主化により、誰もが地球の健康状態を可視化できる時代になりました。あなたのプロジェクトでも、衛星データを活用して地球環境の未来を守る一歩を踏み出してみませんか。

### 地球環境維持のための開発支援
私たちは、環境データ解析プロジェクトを推進するエンジニアを支援しています。

*   **ライトプラン**: 月額 5,000円
*   **スタンダードプラン**: 月額 30,000円
*   **エンタープライズプラン**: 月額 100,000円

以下のリンクからご支援-ご契約いただけます：
[https://buy.stripe.com/test_placeholder](https://buy.stripe.com/test_placeholder)
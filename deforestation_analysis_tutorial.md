# ChatGPTと衛星データで解き明かす：森林減少と環境変化の可視化チュートリアル

リモートセンシング（遠隔探査）技術の進化により、地球上のどこで森林が失われ、環境がどう変化しているかを誰でも分析できるようになりました。本記事では、Sentinel-2衛星データとPython、そしてChatGPTを活用して、環境変化を可視化するワークフローを解説します。

---

## 1. リモートセンシングデータ分析の基礎

リモートセンシングとは、衛星や航空機に搭載されたセンサーを用いて、対象物に触れることなく地表の情報を取得する技術です。特に**Sentinel-2**（欧州宇宙機関）は、10m単位の高解像度で地表を観測しており、植生の状態を把握するのに最適な「NDVI（正規化植生指数）」などの計算が可能です。

## 2. Pythonを用いた分析ワークフロー

環境変化の分析には、以下のライブラリが不可欠です。

*   **Rasterio**: 衛星画像（GeoTIFFなど）の読み込み-書き込み-処理。
*   **Geopandas**: 地理空間データの操作と空間演算。
*   **Matplotlib / Rasterio.plot**: 可視化。

### 基本的な構成
1.  **データ取得**: Google Earth Engine (GEE) 等から対象期間の画像をダウンロード。
2.  **前処理**: 座標系の統一とトリミング。
3.  **解析**: NDVI計算（ `(NIR - Red) / (NIR + Red)` ）による植生密度の算出。
4.  **変化検出**: 時系列データ（例：2020年と2024年）の差分計算。

## 3. ChatGPTを「コーディング-パートナー」にする

複雑な地理空間解析のコードを一から書く必要はありません。ChatGPTに以下のようなプロンプトを投げることで、効率的にスクリプトを生成できます。

**プロンプト例:**
> 「Pythonのrasterioを使って、2つのSentinel-2のGeoTIFFファイル（2020年と2024年）を読み込み、NDVIを計算してその差分をヒートマップとしてプロットするスクリプトを書いてください。また、結果をGeoTIFFとして保存してください。」

**ChatGPTの利点:**
*   **エラーハンドリング**: 座標系が一致しない場合などの例外処理も提案してくれる。
*   **ライブラリの選定**: 文脈に最適なライブラリ（`xarray`や`rioxarray`など）を提案してくれる。

## 4. 実践的な可視化のステップ

1.  **データ準備**: Google Earth EngineのPython API (`earthengine-api`) を使い、特定の領域の画像をエクスポートします。
2.  **コードの実行**: ChatGPTから出力されたコードをJupyter Notebook上で実行し、画像を確認します。
3.  **解釈**: 
    *   **NDVI値が低下している場所**：森林伐採や開発の可能性。
    *   **NDVI値が増加している場所**：植林や季節要因。
4.  **重ね合わせ**: `geopandas`を使用して、保護区の境界データと重ねることで、保護区内での違法伐採を特定することも可能です。

## 5. 結論

ChatGPTとオープンな衛星データを組み合わせることで、環境変化のモニタリングは「専門家だけのもの」から「誰もができる分析」へと変わりました。技術を学ぶことは、地球の未来を守るための第一歩です。ぜひ、興味のある地域のデータから解析を始めてみてください。

---

## 地球環境レジリエンス維持のための活動支援
本記事のような技術情報の提供や、環境保護のためのデータ活用支援を行っています。活動にご賛同いただける方は、ぜひサポートをお願いいたします。
https://ko-fi.com/toai_system
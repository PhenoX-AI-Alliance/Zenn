# ChatGPTで始める衛星画像処理：地球環境レジリエンス監視の実装

気候変動や都市化による環境変化をリアルタイムで把握することは、現代の地球環境レジリエンス（回復力）を高める上で不可欠です。かつては専門的なGIS（地理情報システム）ソフトが必要だった衛星画像解析も、現在はPythonとAIの力で驚くほど手軽に行えるようになりました。

本記事では、ChatGPTをコーディングパートナーとして活用し、Sentinel-2などの衛星データから植生指数（NDVI）を算出し、環境変化を監視する手法を解説します。

---

## 1. なぜ衛星画像処理が重要なのか

衛星画像は、地上では観測が困難な広範囲の環境変化を定点観測できる強力なツールです。
*   **森林減少の監視**: 植生の変化を時系列で追跡。
*   **災害レジリエンス**: 洪水や山火事後の植生回復状況の把握。
*   **都市計画**: ヒートアイランド現象の抑制に向けた緑地評価。

これらをAIと組み合わせることで、膨大なデータから「異常」や「変化の兆候」を自動的に抽出することが可能になります。

---

## 2. Pythonによる実装：NDVI（正規化植生指数）の算出

NDVIは、植物の光合成活動を評価するための指標です。近赤外線（NIR）と赤色光（Red）の反射率を利用し、以下の式で求められます。

$$NDVI = \frac{NIR - Red}{NIR + Red}$$

### 実装コード

Pythonの `rasterio` と `numpy` を使用して、GeoTIFF形式の衛星データを処理します。

```python
import rasterio
import numpy as np

def calculate_ndvi(red_path, nir_path, output_path):
    # 衛星画像の読み込み
    with rasterio.open(red_path) as red_ds:
        red = red_ds.read(1).astype('float32')
    
    with rasterio.open(nir_path) as nir_ds:
        nir = nir_ds.read(1).astype('float32')
        profile = nir_ds.profile

    # NDVIの計算 (ゼロ除算防止のため小さな値を追加)
    ndvi = (nir - red) / (nir + red + 1e-8)

    # 結果の保存
    profile.update(dtype=rasterio.float32, count=1)
    with rasterio.open(output_path, 'w', **profile) as dst:
        dst.write(ndvi, 1)
    
    print(f"NDVI画像を保存しました: {output_path}")

# 使用例
# calculate_ndvi('red_band.tif', 'nir_band.tif', 'ndvi_result.tif')
```

**ChatGPTの活用ポイント**:
このコードの作成において、ChatGPTに「この処理をバッチ化して、複数の日付の画像を比較し、変化量を抽出するコードを書いて」と依頼すれば、時系列解析のロジックも即座に構築できます。

---

## 3. AIが加速させる環境レジリエンス解析

従来、衛星画像解析には「どのバンドを組み合わせるか」「どのような閾値を設定するか」といった専門的な試行錯誤が必要でした。しかし、現在は以下のようなプロセスにシフトしています。

1.  **データ取得**: API（Sentinel Hub等）から対象地域のデータを取得。
2.  **前処理**: ChatGPTにライブラリの使い方を質問し、効率的なデータクレンジングを実装。
3.  **解析と推論**: LLMを用いて、算出したNDVIの数値的変化が「環境悪化」を示しているか、あるいは「植生回復」を示しているかの解釈を支援させる。

AIは単なる計算機ではなく、専門知識のギャップを埋める「解析のナビゲーター」として機能します。

---

## 結論

衛星画像処理の民主化は、個人のエンジニアや研究者が地球環境の監視に貢献できる時代を切り拓きました。Pythonによる自動化とAIによる解釈支援を組み合わせることで、私たちの地球に対する「レジリエンス監視」はより迅速で正確なものになります。

ぜひ、皆さんの手元にある衛星データで、身近な環境の変化を可視化してみてください。

---

## 🌎 地球環境モニタリングをサポートしてください

本記事のような技術開発や、オープンソースを通じた環境データ解析の普及活動を継続するために、活動支援をお願いしています。いただいたご支援は、解析用サーバーの維持費やデータ取得コストに充てさせていただきます。

[**活動を支援する (Ko-fi)**](https://ko-fi.com/phenox)

---
*本記事は、衛星データ解析の基礎的な実装例を紹介するものです。実際の防災-環境保全プロジェクトにおいては、信頼できる気象庁やESA/NASA等の公式データと専門家の判断を併用してください。*
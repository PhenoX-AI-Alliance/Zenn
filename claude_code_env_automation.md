# Claude CodeのPlanモードをループエンジニアリングで自動化し、地球環境データ解析の効率を10倍にする手法

気候変動という人類最大の課題に対し、我々エンジニアが武器にできるのは「データ」と「計算能力」です。しかし、NASAやJAXAが提供する膨大な衛星データ（NetCDFやHDF5形式など）を解析し、有意義なインサイトを導き出すには、膨大なコードの試行錯誤が必要です。

本記事では、Anthropicが提供するCLIツール「Claude Code」の`plan`モードを核とし、ループエンジニアリングの手法を用いて**地球環境データ解析パイプラインを自動化-高速化する手法**を解説します。

---

## 1. Claude CodeのセットアップとPlanモードの自動化

Claude Codeは、ターミナル上で直接AIがコードを書き、実行し、修正するツールです。まずはインストールを行い、環境を整えます。

```bash
npm install -g @anthropic-ai/claude-code
claude config set defaultPlanMode true
```

### ループエンジニアリングの概念
単純なプロンプトでは、複雑な地球科学モデルの構築は困難です。そこで、「Plan → Execute → Evaluate → Refine」というフィードバックループをClaude Codeの`--plan`フラグで強制的に実行させます。

## 2. NASA/JAXAオープンデータの統合

解析対象として、NASAの[Earthdata](https://earthdata.nasa.gov/)やJAXAの[G-Portal](https://gportal.jaxa.jp/)から取得したデータを活用します。

### 実装のポイント
Claude Codeに対して、データ構造を理解させるためのメタデータファイルを配置します。

```bash
# Claudeへの指示例
claude "NASA GPMデータのNetCDFファイルを読み込み、指定した座標の降水量を抽出するPythonスクリプトを書いて。xarrayとdaskを使用してメモリ効率を最適化すること。"
```

Claude Codeは自動的にライブラリの依存関係をチェックし、実行環境を構築します。ここで重要なのは、**データのパスとメタデータを定義した`data_schema.json`をプロジェクト直下に置くこと**です。これにより、AIがデータ構造を推論する時間を短縮できます。

## 3. 再帰的リファインメントループの実装

解析コードの精度を上げるために、以下のスクリプト（`agent_loop.sh`）を作成し、Claude Codeをループさせます。

```bash
#!/bin/bash
# 再帰的リファインメントループ
for i in {1..5}; do
  echo "Iteration $i: Analyzing climate data..."
  claude --plan "解析精度を評価し、エラーがあれば修正して。特にデータ欠損値の処理を改善せよ。"
  if [ $? -eq 0 ]; then
    echo "Iteration $i completed successfully."
    break
  fi
done
```

このループにより、AIは一度の出力で満足せず、境界値テストや統計的な外れ値チェックを自律的に繰り返します。これにより、従来の手動コーディングと比較して**解析パイプラインの構築スピードを10倍に引き上げることが可能**です。

## 4. 結論：気候データ処理の未来

Claude Codeを用いたループエンジニアリングは、単なる自動化ツールではありません。それは、気候科学者が「コードを書くこと」から解放され、「データから未来を予測すること」に集中するための強力なエンジニアリング-パートナーです。

この手法により、数週間かかっていた衛星データの解析パイプライン構築が数時間で完了するようになります。地球環境のレジリエンスを高めるためには、この「解析の高速化」が極めて重要です。

---

## 地球環境レジリエンス構築を支援してください

私たちは、このオープンな解析技術をさらに発展させ、世界中の気候変動対策プロジェクトに提供することを目指しています。この取り組みを支援していただける方は、以下の月額プランへのご参加をお願いいたします。

[**地球環境レジリエンス構築支援のための月額プラン（Stripe）**](https://buy.stripe.com/example_link)

皆様の支援が、より精度の高い気候モデルの開発と、地球の未来を守るためのデータ基盤を支えます。
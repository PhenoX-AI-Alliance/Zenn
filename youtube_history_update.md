# 🎥 YouTube自動更新システムをアップデート：履歴JSONの導入で実現する堅牢なコンテンツ管理

こんにちは！PhenoX-AI-Allianceです。

私たちが開発・運用を進めている総合自動化システム「TOAI_System」において、YouTubeの自動更新機能に関する新しいアップデートを行いました。

今回のコミットでは、新たに **`youtube_history.json`** を導入し、YouTube関連の自動処理の履歴をデータとして永続化・管理する仕組みを実装しました。

* **リポジトリ**: [PhenoX-AI-Alliance/TOAI_System](https://github.com/PhenoX-AI-Alliance/TOAI_System)
* **該当コミット**: [0e6f9b79ea7657bb22b42c0c5611e06ba3e644fc](https://github.com/PhenoX-AI-Alliance/TOAI_System/commit/0e6f9b79ea7657bb22b42c0c5611e06ba3e644fc)

本記事では、この履歴JSON追加の背景と、GitHub Actionsやスクリプトを駆使したYouTube連携の自動化の仕組みについて解説します。

---

## 1. 背景：なぜ「YouTubeの更新履歴」が必要だったのか？

YouTube Data APIや各種スクリプトを用いて、動画の自動投稿やメタデータの更新を完全自動化しているシステムでは、以下のような課題に直面しがちです。

* **二重処理の防止**: すでに処理済みの動画に対して、誤って同じアクション（通知、メタデータ書き換えなど）を繰り返してしまう。
* **状態の可視化**: 「いつ、どの動画に対して、どのような自動処理が実行されたのか」のログや足跡が残らないため、トラブルシューティングが難しい。
* **ステートレスな環境の限界**: GitHub ActionsなどのCI/CD環境は基本的にステートレス（毎回環境がクリーンアップされる）なため、前回の実行結果を引き継ぐ仕組みが必要。

これらを解決するため、処理履歴をJSONファイルとしてリポジトリ内に保持し、自動更新のたびに読み書きする仕組みを導入しました。

---

## 2. 実装の仕組み：GitHub Actions × 履歴JSON

今回のアップデートによる自動化のフローは以下の通りです。

1. **トリガーの起動**
   * GitHub Actions（Cronスケジュールまたは手動ディスパッチ）が実行されます。
2. **履歴JSON（`youtube_history.json`）の読み込み**
   * 過去に処理した動画のIDやタイムスタンプ、ステータスが記録されたJSONファイルをスクリプトが読み込みます。
3. **YouTube API等との連携・差分チェック**
   * 最新のYouTubeチャンネル情報や動画リストを取得し、履歴JSONに存在しない新規動画や、更新が必要なアイテムを特定します。
4. **自動処理の実行**
   * 必要な処理（データベースへの登録、通知、メタデータの更新など）を実行します。
5. **履歴の更新と自動コミット**
   * 処理が成功すると、`youtube_history.json` に今回の処理結果が追記されます。その後、GitHub Actionsの権限（GHO_TOKENなど）を用いて、変更されたJSONファイルを自動でリポジトリにコミット＆プッシュします。

これにより、次回の実行時には最新の履歴が必ず引き継がれる堅牢なループが完成します。

---

## 3. 自動化システムにおける技術的メリット

このような「JSONファイルを状態管理に使うアプローチ」には、次のようなメリットがあります。

* **軽量かつシンプル**: 大規模なデータベース（RDBなど）を別途用意する必要がなく、リポジトリの容量だけで完結するため、インフラコストがほぼかかりません。
* **高い透明性**: 履歴がJSON（テキスト）で管理されるため、Gitの履歴（`git log`）を見るだけで「いつ自動処理の仕様が変わったか」「どの動画でエラーが起きたか」がコードの差分として一目でわかります。
* **GitHub Actionsとの抜群の相性**: ファイルの読み書きからコミットまでをひとつのワークフロー内で完結させやすく、CI/CDの自動化パーツとして非常に扱いやすいです。

---

## まとめ

今回は、TOAI_SystemにおけるYouTube自動更新機能への履歴JSON追加についてご紹介しました。

小さな改善の積み重ねですが、こうした「状態の保持」を組み込むことで、自動化システムの安定性やデバッグのしやすさは劇的に向上します。PhenoX-AI-Allianceでは、今後もAIと自動化を組み合わせた効率的なシステム開発の知見を共有していきます。

引き続き、TOAI_Systemの応援をよろしくお願いいたします！

---

## 支援・サポートのお願い

PhenoX-AI-Allianceのオープンソース活動や自動化システムの開発継続のため、もしよろしければご支援をお願いいたします。いただいたご支援は、API利用料や開発環境の維持に活用させていただきます。

- [☕ Ko-fiで支援する](https://ko-fi.com/phenox)
- [💳 Stripeでサポートする（一回払い等）](https://buy.stripe.com/14k9B2g886vK1q0001)